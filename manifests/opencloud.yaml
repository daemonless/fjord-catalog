name: opencloud
services:
  opencloud:
    image: ghcr.io/daemonless/opencloud:latest
    container_name: opencloud
    restart: unless-stopped
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - OC_URL=${OC_URL}
    extra_hosts:
      - "hostname:127.0.0.1"
    volumes:
      - "${CONFIG_DATA}:/config"
    ports:
      - "${WEB_PORT}:9200"
x-fjord:
  version: "0.1"
  info:
    id: opencloud
    name: OpenCloud
    description: OpenCloud is a modern file-sync and share platform.
    category: Utilities
    class: service
    icon: /catalog/icons/opencloud.svg
    upstream_url: https://github.com/daemonless/opencloud
    web_url: https://opencloud.eu/
  variables:
    - name: WEB_PORT
      label: Web UI
      type: port
      default: "9200"
    - name: CONFIG_DATA
      label: Configuration directory
      type: zfs_dataset
      default: config
      host_permissions:
        uid: 1000
        gid: 1000
        mode: "755"
    - name: PUID
      label: User ID for the application process
      type: string
      default: "1000"
    - name: PGID
      label: Group ID for the application process
      type: string
      default: "1000"
    - name: TZ
      label: Timezone for the container
      type: string
      default: UTC
    - name: OC_URL
      label: URL where OpenCloud can be accessed. Must match EXACTLY what you type in the browser (protocol + hostname + port) — a mismatch (e.g. short hostname vs FQDN) causes a 'Missing or invalid config' error in the web UI with no other explanation.
      type: string
      default: https://hostname:9200
  variants:
    - id: latest
      label: Upstream binary
      default: true
