name: heimdall
services:
  heimdall:
    image: ghcr.io/daemonless/heimdall:latest
    container_name: heimdall
    restart: unless-stopped
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - "${CONFIG_DATA}:/config"
    ports:
      - "${WEB_PORT}:80"
x-fjord:
  version: "0.1"
  info:
    id: heimdall
    name: Heimdall
    description: An Application dashboard and launcher — organize all your web apps and services in one place.
    category: Utilities
    class: service
    icon: /catalog/icons/heimdall.svg
    web_port: ${WEB_PORT}
    upstream_url: https://github.com/linuxserver/Heimdall
    web_url: https://heimdall.site/
    only_for_archs:
      - amd64
      - aarch64
  variables:
    - name: WEB_PORT
      label: Web UI
      type: port
      default: "80"
    - name: CONFIG_DATA
      label: Configuration directory (database, .env, storage)
      type: zfs_dataset
      default: config
      host_permissions:
        uid: 1000
        gid: 1000
        mode: "755"
    - name: PUID
      label: User ID for the application process
      type: string
      default: "1000"
    - name: PGID
      label: Group ID for the application process
      type: string
      default: "1000"
    - name: TZ
      label: Timezone for the container
      type: string
      default: UTC
  variants:
    - id: latest
      label: Upstream binary
      default: true
